regex
  delimiters / /
  literals
  boundaries ^ $
  regex modifiers /gi
  as many as {,}
  match modifiers * + ? .
  grouping () [] |
  special \d \s \w

methods
  test    :: bool
  match   :: []
  search  :: int
  split   :: []
  replace :: string
