regex
  delimiters / /
  literals
  boundaries ^ $
  regex modifiers /gi
  as many as {,}
  match modifiers * + ? .
  grouping () [] |
  special \d \s \w

methods
  test    :: bool
  exec	  :: []
  
  search  :: int
  split   :: []
  replace :: string
  match   :: []
